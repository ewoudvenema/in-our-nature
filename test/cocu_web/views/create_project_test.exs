defmodule CocuWeb.CreateProjectTest do
  use CocuWeb.FeatureCase, async: true

  #import Wallaby.Query, only: [css: 2]
  #import Wallaby.Browser

  #test "Button discover projects", %{session: session} do
  #  text = "Launch a Project"
  #  session
  #  |> visit("/")
  #  |> assert_has(css(".main-banner > .row > .button", text: text))
  #end
end